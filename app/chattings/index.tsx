import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const ChattingsScreen = (props: Props) => {
  return (
    <View>
      <Text>ChattingsScreen</Text>
    </View>
  );
};

export default ChattingsScreen;

const styles = StyleSheet.create({});
