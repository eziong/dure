import { StyleSheet, Text, View } from "react-native";
import React from "react";
import PostsBody from "@src/components/posts/add";
import AddPost from "@src/components/posts/add";

type Props = {};

const AddPostScreen = (props: Props) => {
  return <AddPost />;
};

export default AddPostScreen;

const styles = StyleSheet.create({});
