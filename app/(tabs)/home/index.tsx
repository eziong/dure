import { StyleSheet, Text, View } from "react-native";
import React from "react";
import HomeBody from "@src/components/home/body";

type Props = {};

const HomeScreen = (props: Props) => {
  return <HomeBody />;
};

export default HomeScreen;

const styles = StyleSheet.create({});
