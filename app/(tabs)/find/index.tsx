import { StyleSheet, Text, View } from "react-native";
import React from "react";
import FindBody from "@components/find/body";

type Props = {};

const FindScreen = (props: Props) => {
  return <FindBody />;
};

export default FindScreen;

const styles = StyleSheet.create({});
