import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const NearHeader = (props: Props) => {
  return (
    <View>
      <Text>NearHeader</Text>
    </View>
  );
};

export default NearHeader;

const styles = StyleSheet.create({});
